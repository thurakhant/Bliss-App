import 'package:flutter/material.dart';

class SechdulePage extends StatelessWidget {
  const SechdulePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Sechdule'),
      ),
    );
  }
}
